# EdgeBlocks website -> EventEdge API proxy (optional)
# If unset, the website API routes serve placeholders.

EVENTEDGE_API_BASE=https://your-eventedge-api.example.com
# Optional bearer token (either "Bearer xxx" or just "xxx")
EVENTEDGE_API_TOKEN=
